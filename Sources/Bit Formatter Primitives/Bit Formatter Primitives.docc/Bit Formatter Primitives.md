# ``Bit_Formatter_Primitives``

@Metadata {
    @DisplayName("Bit Formatter Primitives")
    @TitleHeading("Swift Primitives")
}

Base-2 text rendering of bit data: `Bit.Formatter` presents the bits of a
`Bit.Pattern<Carrier>.Mask` as a fixed-width `'0'`/`'1'` string, honoring
`Bit.Order` and optional digit grouping.

## Topics

### Formatting Bits

- ``Bit/Formatter``
